# Interpark 예약 연습 UI — 구현 지시서 (Antigravity용)

## 목표
- `i_ticket.html` 1개 파일 기준으로 **독립 화면**으로 구현.
- 인터파크 UI를 **픽셀 유사 수준**으로 재현.
- 좌석 단계는 **캡차 → 구역선택 → 좌석선택** 3단계로 명확히 분리.
- **핵심 버튼만 동작**, 나머지는 알림/비활성 처리.
- **데스크톱 우선 + 반응형**(모바일에서도 깨지지 않게).

## 기준 스크린샷 (정답)
- 보안문자(음영 처리 상태): src/interpark-example/스크린샷 2026-02-01 오후 6.17.41.png
- 좌석 구역 선택: src/interpark-example/스크린샷 2026-02-01 오후 6.17.49.png
- 좌석 선택 상세: src/interpark-example/스크린샷 2026-02-01 오후 4.26.37 복사본.png
- 가격/할인 선택: src/interpark-example/스크린샷 2026-02-01 오후 4.27.04 복사본.png
- 배송/주문자 확인: src/interpark-example/스크린샷 2026-02-01 오후 4.27.13 복사본.png
- 결제하기: src/interpark-example/스크린샷 2026-02-01 오후 4.27.23 복사본.png

## 구현 범위
- **프론트만 구현** (백엔드 없음).
- 데이터는 **mock 값**으로 고정.
- `i_ticket.html` + `i_ticket.css` + `i_ticket.js` 중심으로 구성.

## 전역 제약
- 기존 전역 레이아웃/스타일과 분리되도록 `i_ticket.html`을 단독 HTML로 구성.
- `layout:decorate` 제거.
- 페이지 전체 구조/스타일/스크립트를 **이 화면 안에서 완결**.

---

# 화면 단계별 명세 (체크리스트)

## A. 캡차 오버레이 (초기 로딩)
**레이아웃**
- 좌석 구역 선택 화면이 **배경에 보이되**, 전체 화면 음영 처리(반투명).
- 중앙에 캡차 모달(이미지와 동일한 크기/여백/폰트/버튼 스타일).

**요소**
- 캡차 이미지 영역(노이즈 배경 + 문자).
- `새로고침` 버튼 (아이콘).
- `입력완료` 버튼.

**동작**
- 캡차 성공 전: 좌석 구역 선택 클릭 불가(배경 비활성).
- `입력완료`: 정답이면 오버레이 제거 → 구역 선택 활성화.
- `새로고침`: 캡차 문자/이미지 새로 생성.
- 실패 시 경고 메시지 + 입력 초기화.

---

## B. 좌석 구역 선택 (Image #2)
**레이아웃**
- 좌측: 원형 좌석 구역 맵 + 안내문/주의문구.
- 우측: 좌석등급/잔여석, 선택좌석 표시 영역.

**요소**
- 원형 구역 맵(구역 번호 텍스트 포함).
- 안내 문구 빨간색 경고 영역.

**동작**
- 구역 클릭 시 → **좌석 선택 화면**으로 전환.
- 이 단계에서 `좌석선택완료`는 **없음**.
- `좌석도 전체보기`는 이 단계가 기본 상태.

---

## C. 좌석 선택 상세 (Image #3)
**레이아웃**
- 좌측: 좌석 배치도(격자), 좌석 행 번호 표시.
- 우측: 선택좌석 리스트 + 좌석 등급 표시.

**핵심 버튼**
- `좌석선택완료`
- `좌석도 전체보기`
- `좌석 다시 선택`

**동작**
- 좌석 클릭 → 선택/해제 토글.
- 선택 시 우측 리스트/총액 업데이트.
- `좌석선택완료`: 가격/할인 단계로 이동.
- `좌석도 전체보기`: 구역 선택으로 이동.
- `좌석 다시 선택`: 선택 좌석 초기화.

---

## D. 가격/할인 선택 (4.27.04)
**레이아웃**
- 좌측: 가격 테이블(좌석등급/권종/가격/수량).
- 하단: 쿠폰/할인 영역.
- 우측: 요약 패널 동일.

**핵심 버튼**
- `다음단계`

**동작**
- 수량 변경 → 총액 반영.
- 쿠폰/부가 버튼은 알림 토스트 처리.
- `다음단계`: 배송/주문자 확인 단계 이동.

---

## E. 배송/주문자 확인 (4.27.13)
**레이아웃**
- 좌측: 배송방법 선택, 프리미엄 서비스.
- 우측: 예매자 확인 폼.

**핵심 버튼**
- `이전단계`, `다음단계`

**검증 규칙 (실제 수준)**
- 이름 비어있으면 실패.
- 생년월일 6자리 숫자 검증.
- 연락처 3-4-4 숫자 검증.
- 이메일 형식 검증.

**동작**
- `다음단계`: 검증 통과 시 결제 단계 이동.
- `이전단계`: 가격/할인 단계로 복귀.

---

## F. 결제하기 (4.27.23)
**레이아웃**
- 좌측: 결제수단 선택 리스트.
- 우측: 카드 종류/할인 정보 영역.

**핵심 버튼**
- `다음단계` (최종 결제)

**검증 규칙 (실제 수준)**
- 결제수단 미선택 시 실패.
- 카드 종류 미선택 시 실패.

**동작**
- `다음단계` 클릭 시 완료 알림 후 종료.

---

# 공통 UI / 동작 규칙
- **핵심 버튼만 동작**.
- 부가 버튼은 `알림 토스트`로 통일: “연습모드입니다.”
- 우측 요약 패널의 구조/색상/폰트는 스크린샷과 일치.
- 금액/좌석수는 JS 상태로 관리.

# 상태 머신
- `CAPTCHA` → `AREA_SELECT` → `SEAT_SELECT` → `PRICE_DISCOUNT` → `DELIVERY_ORDER` → `PAYMENT`
- 단계 이동은 핵심 버튼 조건 충족 시에만.

# Mock 데이터 (고정값)
- 공연명: FAM + ILY : FAMILY : FAM I LOVE YOU
- 장소: KSPO DOME
- 날짜/시간: 2026년 02월 06일(금) 19:00
- 티켓금액: 88,000원
- 수수료: 2,000원
- 총 결제금액: 90,000원

# 체크리스트 (검수용)
- 캡차 성공 전 배경 클릭 불가
- 캡차 성공 후 음영 해제
- 구역 클릭 → 좌석 선택 화면 전환
- 좌석 선택 시 리스트/금액 갱신
- 좌석선택완료 → 가격/할인 이동
- 배송/주문자 입력 검증 동작
- 결제수단/카드 선택 검증 동작
- 반응형에서 화면 잘림 없이 유지
