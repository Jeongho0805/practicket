-- V6 이 넣은 예시 공지 5건을 지운다.
--
-- V6 은 landing.html 하드코딩분을 옮겨 담은 것이라 내용이 전부 가상이다
-- ("서버 점검 7/2", "대기열 진행률 멈춤" 등 실제로 없던 일).
-- 실서비스에 그대로 나가면 안 되므로 제거한다.
--
-- V6 의 INSERT 를 직접 지우지 않는 이유: 이미 적용된 환경(stage)에서 체크섬이 어긋나
-- Flyway validate 가 실패하고 앱이 부팅되지 않는다.
--
-- 제목으로 지우는 이유: id 는 환경마다 다를 수 있다.
-- 이미 손으로 지운 환경에서는 0건이 지워지고 끝난다(여러 번 돌아도 안전).

DELETE FROM `notice` WHERE `title` IN (
  '서버 점검 안내 — 7/2(수) 새벽 2시~4시',
  '대기열 진행률이 간헐적으로 멈추던 문제 수정',
  '랭킹 기록이 일부 누락되던 현상 개선',
  '야구 티켓팅 서비스 오픈 준비 안내',
  '모바일에서 좌석 선택이 밀리던 문제 수정'
);
