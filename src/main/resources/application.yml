spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    hikari:
      keepalive-time: 30000
  jackson:
    property-naming-strategy: SNAKE_CASE
server:
  servlet:
    session:
      cookie:
        name: "auth"
        http-only: false
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
